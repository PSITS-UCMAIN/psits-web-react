---
name: analyze-design
description: Analyzes a client-side codebase folder — its components, styles, tokens, layout patterns, typography, color usage, animation, and visual conventions — and produces a structured DESIGN.md that documents the design system as it actually exists in code. Use this skill whenever the user asks to "analyze the design", "document the UI style", "generate a DESIGN.md", "audit the frontend", "capture the design language", "extract design tokens", or wants a reference doc for how the client-side looks and behaves — even if they don't use the word "design". Also triggers when a new developer needs onboarding into the visual conventions of an existing UI codebase, or when an AI agent needs a design reference before building new components.
---

# Canonical Reference

For the complete skill implementation, see: [.agents/skills/analyze-design/SKILL.md](../../.agents/skills/analyze-design/SKILL.md)